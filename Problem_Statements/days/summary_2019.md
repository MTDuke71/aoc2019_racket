# AoC 2019 Summary

| Day | Title | Parse | Part 1 | Part 2 | Total | Algorithm | Notes |
|-----|-------|-------|--------|--------|-------|-----------|-------|
| 01 | The Tyranny of the Rocket Equation | 0.0141 ms | 0.0006 ms | 0.0029 ms | 0.0175 ms | Fixed-point iteration (Part 2) | P1 3481005 / P2 5218616; platform set up this day |
| 02 | 1202 Program Alarm | 0.1050 ms | 0.0050 ms | 27.0500 ms | 27.1600 ms | Bytecode interpreter; brute-force grid search (Part 2, affine closed form sidebar) | P1 4945026 / P2 5296; first Intcode VM + first mutable vector |
| 03 | Crossed Wires | 0.5200 ms | 92.2400 ms | 92.2750 ms | 185.0350 ms | Spatial hash (occupancy map) + set intersection; segment-intersection sidebar | P1 2180 / P2 112316; first immutable hash; trace-once variant (day03a) halves solve to ~92 ms (2.0×); mutable-hash measured a dead-end |
| 04 | Secure Container | 0.0000 ms | 38.5000 ms | 38.1000 ms | 76.6000 ms | Run-length encoding + brute-force range scan; stars-and-bars (combinations-with-repetition) closed-form sidebar | P1 1675 / P2 1142; first RLE; non-decreasing via variadic `(apply <= ds)`; parts differ only by `>=` vs `=` |
| 05 | Sunny with a Chance of Asteroids | 0.5740 ms | 0.0255 ms | 0.0270 ms | 0.6265 ms | Bytecode interpreter (Intcode) extended: parameter modes, I/O, conditional jumps | P1 6731945 / P2 9571668; Intcode #2; opcode = `instr mod 100`, modes via digit extraction; PC-as-loop-variable so jumps are just a different next-ip; input is a self-modifying per-subsystem test selector (see day05_disassembly.md — static descent defeated, dynamic trace required) |
| 06 | Universal Orbit Map | 0.4940 ms | 6.2540 ms | 0.0440 ms | 6.7920 ms | Parent-pointer tree: sum of depths (Part 1), lowest common ancestor via chain intersection (Part 2); memoized-depth O(n) sidebar | P1 140608 / P2 337; first `for/hash` / `in-naturals` / `for/first`; "orbits exactly one" collapses graph to rooted tree — no BFS needed; P1 is O(n·d) re-walk (5.7 ms, the day's whole cost), P2 is two chains + one hash (µs) |
| 07 | Amplification Circuit | 0.4400 ms | 21.0650 ms | 25.3400 ms | 46.8450 ms | Intcode amplifier chain: permutation brute force (Part 1 series), cooperative VM scheduler with feedback loop (Part 2); `run/inputs` + growable memory | P1 46014 / P2 19581200; five Intcode copies; Part 1 phases 0–4, Part 2 phases 5–9; `vm-step!` blocks on empty input queue |
| 08 | Space Image Format | 0.2815 ms | 0.4950 ms | 0.2480 ms | 1.0245 ms | 1-D → (layer,row,col) reshape: per-layer histogram (Part 1), front-to-back transparency resolve + glyph render (Part 2) | P1 1677 / P2 UBUFP; 100 layers of 25×6; first `for/vector` / `vector-count` / `argmin`; flat offset L·W·H + r·W + c; Part 2 is `for/first` non-2 down the layer axis |
| 09 | Sensor Boost | 0.8240 ms | 0.1000 ms | 53.3680 ms | 54.2920 ms | Intcode finale: relative addressing mode (mode 2) + relative base (opcode 9) added to the Day 5 VM; quine as the correctness oracle | P1 4080871669 / P2 75202; the *complete* Intcode computer; relative mode = one new `val` clause + mirror in `addr` + opcode 9, eight handlers untouched (operand-resolution is the orthogonal axis); `rb` threaded like `ip`; big memory + bignums free from Day 5/7 primitives; P2 (~530× P1) runs a heavy hidden computation the self-test only warmed up |
| 10 | Monitoring Station | 0.0000 ms | 13.8000 ms | 14.1400 ms | 27.9400 ms | Primitive direction vectors (gcd-reduced sightlines) for the visibility count (Part 1); clockwise angular sweep via `atan2` + round-robin over rays (Part 2) | P1 230 (best station 19,11) / P2 1205 (200th = 12,5); first lattice-geometry day; visibility = count of *distinct* gcd-reduced directions, collinear asteroids collapse in a `set`; laser order = group-by-direction → sort near→far → order rays by `atan(dx, -dy)` clockwise-from-up → round-robin one head per ray per rotation; both parts O(n²) over ~310 asteroids (each `best` runs ~310 visibility scans); parse below the 100-iter harness resolution; new: `in-indexed`, nested `for*/list`, `argmax`, `hash-update!`, two-arg `atan`, `pi` |
| 11 | Space Police | 0.5500 ms | 24.0750 ms | 1.5750 ms | 26.2000 ms | Intcode VM ([Day 9](day09_function_guide.md)) run as a resumable coroutine ([Day 7](day07_function_guide.md)'s `vm-step!` block/resume protocol) driving a hull-painting robot; `(x . y)` hash bookkeeping ([Day 10](day10_function_guide.md)-style); Part 2 renders the painted-white panels as a `#`/`.` grid (no OCR — human reads the letters, per the 2018 Haskell repo's Day 10 stance) | P1 2129 (panels painted at least once) / P2 `PECKRGZL` (registration code); both cross-checked pixel-for-pixel against an independent Python implementation; unpainted panels are absent hash keys (not pre-seeded), so `hash-count` matches the puzzle's own "counted once / never painted the ending panel" callouts exactly; `paint-hull`'s only new parameter is `start-color` (black for P1, white for P2) |
| 12 | The N-Body Problem | 0.0000 ms | 0.4000 ms | 81.7200 ms | 81.9600 ms | Decomposition into independent subsystems recombined by `lcm` (the CRT instinct); symplectic (semi-implicit) Euler integration, whose bijectivity makes the orbit a pure cycle | P1 12351 / P2 380635029877596; the three axes never interact, so this is three 2-D simulations of 4 scalars, not one 6-D one — the only place x/y/z meet is the energy formula; per-axis periods 22958 / 286332 / 231614 (pairwise gcd 2, so lcm = product/4); no `seen` set needed because the step map is invertible (`p = p'−v'`, `v = v'−g(p)`) so the first repeat *must* be the initial state — no ρ-tail; naïve 6-D search would take 3.8×10¹⁴ steps ≈ 1.8 M years at the measured 152 ns/step; parse below the 50-iter harness resolution (0.00915 ms over 20000); half-period-by-time-reversal (`J(p,v)=(p−v,−v)`, `J S J = S⁻¹`, T = 2×first-all-zero-velocity, verified on all 6 axes) sidebar'd; phase-ordering experiment: drift-then-kick is conjugate to the puzzle's map so P2 is bit-identical while P1 breaks (259 vs 179 on ex1), but true explicit Euler (both phases from the old state) is non-symplectic — momentum still conserved, positions blow up unboundedly, P2 never terminates; new: `regexp-match*`, `values`/`define-values`, two-accumulator `for/fold`, `for/lists`, `(apply map list …)` transpose, `sgn` |
| 13 | Care Package | 2.1500 ms | 4.2000 ms | 128.0500 ms | 134.4000 ms | Intcode VM as an arcade cabinet: stream framing (flat outputs → `(x, y, tile)` triples); in-band signaling (the `(-1, 0)` sentinel triple carries the score, not a tile); bang-bang (on-off) control — the player is `(sgn (- ball paddle))` | P1 348 blocks / P2 16999; **first day the VM is imported rather than re-derived** — the instruction set froze at [Day 9](day09_function_guide.md), so `vm-step!` moved verbatim into [src/intcode.rkt](../../src/intcode.rkt) (Day 11's copy stays put: its guide annotates it); free play is a *memory poke* (`vm-poke!` address 0 ← 2), a channel distinct from opcode 3; the joystick is passed as a **thunk**, not a value, so it's evaluated at block time and sees the frame just drawn (Day 11 could pass a value; here many draw commands separate input reads); `play` carries **three integers, not a board** — y is irrelevant (paddle moves horizontally), blocks are irrelevant (they break on contact), so a 1000-cell screen collapses to (score, ball-x, paddle-x); tracking is provably safe because `g' = g + Δball ± 1` never grows `\|g\|` while paddle speed = ball speed — predictive/ballistic aiming is only needed if that parity breaks; **349 score events = 1 initial + 348 bricks** and the board is empty at halt, which proves the win (and derives P1 from P2); 4778 game ticks, 20805 draw commands, 609973 `vm-step!` calls (~4.8 M steps/s); biggest program yet at 2640 cells → the year's slowest parse; cross-checked against an independent Python implementation; new: `->*` with keyword args, `or/c`/`list/c` as a return-value contract (a sum type as a runtime predicate), literal patterns and clause order in `match` |
| 14 | Space Stoichiometry | 0.2990 ms | 0.1005 ms | 2.6575 ms | 3.0570 ms | Topological sort (Kahn's algorithm) over the recipe DAG; binary search on the answer (Part 2, LP-relaxation upper bound sidebar) | P1 628586 / P2 3209254; the day is one clause — "reactions cannot be partially run" — and the whole trick is *when* to round up: accumulate all of a chemical's demand, then take one ceiling, which is exactly what a topological order (edges output→input) guarantees; naive per-demand rounding overpays 47.9% on the real input (929471 vs 628586) and returns 41 instead of 31 on the spec's first example; no surplus/leftover table needed at all — each chemical is produced exactly once, in one batch; P2 is bisection on `affordable?`, legal because `ore-for` is monotone non-decreasing in fuel (induct up the topo order: ceiling is monotone) and bracketed below by the naive rate ⌊10¹²/628586⌋ = 1590872, provably affordable by subadditivity `ore-for(k·f) ≤ k·ore-for(f)`; the true answer 3209254 is **2.017× that rate** — bulk production recovers that much waste (ore/fuel falls 628586 → 311599 and plateaus by f≈10⁴); P2 = **26.4× P1** measured against 26 `ore-for` calls predicted (1 + 2 grow + 23 bisect), the year's cleanest cost-model confirmation; bracket bug found by test: flooring `lo0` at 1 reports 1 FUEL on a budget that can't afford one — binary-search-on-the-answer fails in the *bracket*, not the loop; LP relaxation (drop all ceilings, sweep in exact rationals) gives ORE/FUEL = 3324344305754201/10668672000 and a rigorous upper bound of 3209256 — integrality gap of **2** on the real input, **0** on all three large examples — sidebar'd as an ~8× faster P2 but not shipped (the gap isn't provably bounded); 63 reactions / 64 chemicals, longest FUEL→ORE chain 13, only 5 chemicals made directly from ORE; cross-checked against an independent Python implementation; new: immutable `struct` with `#:transparent`, `match-define`, `hash-ref!`, `hash-update!`, `remove-duplicates`, `(in-hash h)` two-name iteration, `#:unless` for-guard, `(quotient (+ n per -1) per)` ceiling-division |
| 15 | Oxygen System | 0.0740 ms | 66.6240 ms | 66.4620 ms | 133.1600 ms | Depth-first search with an explicit backtrack stack (embodied exploration), then breadth-first search over the recovered map; Part 2 is the ECCENTRICITY of the oxygen cell (a flood fill IS a BFS) | P1 254 / P2 268; **first Python day** and the first with a pytest module (python/tests/test_day15.py, 18 tests) -- the VM is extracted to [python/intcode.py](../../python/intcode.py) mirroring the Racket tree's src/intcode.rkt move at Day 13; the day is one constraint -- the droid's position is state you can only *walk*, never set -- which rules out driving BFS directly (no teleport instruction) and makes the traversal DFS whose `trail` of commands is simultaneously the recursion stack and the route home; a wall probe is *free* (status 0 = the droid did not move, nothing to undo), giving the exact identity `commands = 2*(open-1) + walls` = 2*798 + 860 = **2456**, pinned as a test; the walk is deliberately a MAPPING phase, not a search -- both parts are then plain BFS over the dict, differing only in where the BFS is rooted, so they measure the same to within 0.2%; the input's maze turns out to be a **tree** (799 open cells, 798 edges, degree histogram {1:37, 2:727, 3:35}, zero cycles -- a classic maze-generator carve), which means DFS trail depth to the oxygen *equals* the BFS distance (measured 254 = P1) and a wall-follower would also work -- both deliberately NOT relied on, since the statement promises no such thing and one loop breaks them silently (pinned by a ring counter-example); double-sweep BFS puts the tree diameter at 466 with the droid's own start as one endpoint, while the oxygen is comparatively central (eccentricity 268, 127th of 799; the maze's radius is 233 at (-3,18)); `solve` walks once and answers both for **1.98x** the two parts run separately (66.67 ms vs 131.99 ms); the day is 99% Intcode interpretation (83,123 VM instructions, ~34 per movement command, ~1.26 M instr/s) and 1% graph algorithm (BFS 0.56 ms, below the run-to-run noise floor); state-cloning BFS -- `deepcopy` the VM per frontier cell so 'teleport' becomes 'resume a snapshot' -- is correct and 10.2x slower (674.9 ms), sidebar'd as the technique to reach for when actions are irreversible; cross-checked P1 against that independent method before submitting; **disassembled** (day15_disassembly.md, python/day15_disasm.py): the smallest and most completely understood program of the year -- 70 instructions filling addresses 0-251 with no gaps, no subroutines, no stack, opcode 9 never used, one `while(true)` around a 4-way switch, and an invalid command is the program's ONLY halt; three of its five state variables (`xpar`, `ypar`, `row`) are redundant with (x,y) and exist purely because **Intcode has no division or modulo** -- strength reduction, parity maintained as a toggled bit rather than computed; `a*b` is AND and `a+b` is OR on 0/1 values, there being no bitwise opcodes; the single self-modifying store patches an *operand* (address 211, the load at 210) and never control flow, which is how you index an array on an ISA with no indexed addressing and why static descent is COMPLETE here (the frozen Racket tool reports 72 instructions and declares defeat -- it follows the fall-through of `1105 1 0`, an unconditional jump); **the maze is data, not code**: a 39x20 table at 252 encoding a standard grid-graph maze (cells at odd/odd, pillars at even/even, one entry per edge), so the tree property is explained rather than merely observed -- 399 passages over 400 cells is a spanning tree by definition; the wall bit is a one-bit fact stored as a random 7-bit number and recovered by `< 37`, where **37 is the only integer in 1..99 absent from the table** (so `<37` and `<38` are the only thresholds that work); the oxygen system is a LITERAL pair of compare immediates at addresses 146/153 = (37,39), and the start (21,21) at 1034/1035, giving (16,18) by subtraction -- so **both answers are readable off the disk with the VM never started**, like [Day 13](day13_disassembly.md) but for the opposite reason (Day 13's world was about to be drawn; Day 15's was compiled in); static recovery verified cell-for-cell against the walk and pinned by four more tests (22 total) |
| 16 | Flawed Frequency Transmission | 0.0370 ms | 38.1690 ms | 1989.4360 ms | 2027.6420 ms | Prefix sums (1-D summed-area table) over the pattern matrix's constant-value runs (Part 1); triangularity + suffix sums over the last 8.1% of the signal (Part 2); binomial-convolution closed form via Kummer + Lucas + CRT sidebarred | P1 76795888 / P2 84024125; **the year's slowest day so far, by 11×** (2.03 s vs Day 3's 185 ms) and the first non-Intcode day since [Day 14](day14_function_guide.md); the whole day is refusing to transcribe the prose and looking at the matrix instead — one phase is `out = M @ digits` with `M` **upper triangular** (row k is zero left of column k: that IS "skip the very first value exactly once") and every row a handful of **constant-value runs** (`k−1` zeros, `k` ones, `k` zeros, `k` minus-ones, repeat), and each property alone collapses one part; P1: a run is a contiguous slice total, so one prefix-sum array turns row k's dot product into ~n/2k subtractions instead of n multiply-adds — 2383 slice queries per phase against n²=422500, a **177× operation reduction** measured as **82.1×** wall clock (32.16 → 0.39 ms/phase), and the `min(start+k, n)` that clamps each row's truncated last run is the only place an off-by-one can hide, invisible at the puzzle's 8- and 32-digit example lengths, so `phase` is checked against a literal transcription of the statement at **every** length 1..40; `abs` before `% 10` is load-bearing because Python's `%` is already non-negative (`-17 % 10` is 3, the statement wants 7); answers are **strings** — the statement's own trace lands on `01029498` and `1029498` is a different, rejected answer; P2: the signal is 650×10000 = 6,500,000 digits and the naive route is ~10 years (37 days/phase extrapolated), while the *shipped prefix-sum phase* over the whole signal still measures **14.82 s/phase = 24.7 min** — optimising the phase is the wrong axis entirely; below the midpoint (2k−1 ≥ n) row k is all ones to the end, so a phase degenerates to a plain **suffix sum** (one right-to-left running total, no prefix array, no sign alternation, no `abs` since every partial is non-negative), and triangularity says nothing left of the offset can ever reach the answer — the message offset 5,972,877 sits **91.89% into** the signal, so only the last **527,123 digits (8.110%)** are ever built or touched, via `digits[r:] + digits * (repeats - q - 1)` rather than materialising 6.5 M; **the precondition is a property of the input, not a promise of the statement** — every published example shares it (303673/320000, 293510/320000, 308177/320000) but `part2` raises rather than silently answering when it fails; **problem within the problem**: applying the suffix-sum operator S 100 times is *one* linear operator, and S^k's coefficient is `C(j−i+k−1, k−1)`, so P2 is a single dot product against row 99 of **Pascal's triangle** (verified against `math.comb` for k = 1,2,3,7,100) — and `C(d+99,99) mod 10` needs no big integers at all: **Kummer** gives mod 2 as the single test `(d & 99) == 0` (99 = 0b1100011, four set bits, so ~1 in 16 survive) and **Lucas** gives mod 5 digitwise in base 5 (99 = 344₅, short-circuiting on the first zero factor), recombined by CRT as `(5·m₂ + 6·m₅) mod 10`, checked against `math.comb(m,99) % 10` over 3000 consecutive m; only **40,857 of 527,123 coefficients (7.75%) are nonzero**, so 8 dense dot products run P2 in **268.9 ms (7.4×)** and the sparse version in **~169 ms (11.8×)** including coefficient construction — sidebarred, not shipped, because "a phase over the tail is a suffix sum" is the insight the puzzle is about; P2 is 52× P1 while the inputs are 811× apart in size, because `accumulate` carries the suffix sweep in C (100 prefix-array builds are only 1.46 ms of P1's 38.2 ms — the cost is the 238,300 slice queries); 65 tests, pinning three claims invisible in the puzzle's examples: the prefix-sum phase equals the literal definition at every length 1..40, a below-midpoint phase IS a suffix sum (checked against the general `phase`, not asserted), and scribbling over every digit before the offset leaves the answer byte-identical |
